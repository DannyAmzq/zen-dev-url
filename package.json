{
  "name": "zen-dev-url",
  "version": "1.2.0-beta.1",
  "private": true,
  "author": "DannyAmzq",
  "description": "A development toolbar for Zen Browser with remembered site preferences.",
  "license": "UNLICENSED",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test",
    "check": "node --check devbar.uc.js && npm test",
    "package": "node scripts/package.cjs"
  }
}
